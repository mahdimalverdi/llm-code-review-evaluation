# Abstract

<!-- TODO: After the empirical study is executed, rewrite this abstract in result-oriented language. Replace design-oriented phrases such as "is designed to compare" with concrete findings, sample size, annotation details, and the strongest observed trade-offs. -->

Large language model (LLM) assistants can generate code review comments that appear fluent but are unsupported, irrelevant, technically incorrect, non-actionable, or too low-value to justify reviewer attention. Existing evaluations often report whether generated comments are correct, useful, accepted, or less hallucinatory, but they rarely examine the trade-offs introduced by mitigation strategies. A strategy that reduces problematic comments may also suppress useful weak signals, reduce automatic review coverage, increase human escalation, or add computational cost. This paper presents the design of a controlled empirical study of representative mitigation strategies for problematic LLM-generated code review comments. The study is designed to compare a baseline LLM reviewer, robust prompting, a context-quality gate, post-generation verification, and a hybrid gate-plus-verifier design on a paired set of review instances. To support the comparison, we define an operational taxonomy of problematic comments and an annotation protocol that separates correctness, grounding, relevance, usefulness, actionability, context quality, and mitigation decision. The planned evaluation uses both error-reduction metrics and preservation metrics, including useful comments retained, useful comments wrongly suppressed, review coverage, human escalation, and cost proxies. Rather than seeking a universal winning strategy, the study is designed to characterize which interventions reduce which failure types and what hidden costs accompany those reductions. The paper contributes a trade-off-oriented evaluation design for LLM-based code review tools by treating mitigation as a workflow decision rather than a single quality score.
